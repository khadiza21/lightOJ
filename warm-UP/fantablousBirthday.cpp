#include <bits/stdc++.h>
using namespace std;
int main()
{
    int cases, x, y, seconds;
    cin >> cases;
    for (int i = 0; i < cases; i++)
    {
        cin >> seconds;
        cout << "case " << i + 1 << ": " << x << " " << y << endl;
    }

    return 0;
}